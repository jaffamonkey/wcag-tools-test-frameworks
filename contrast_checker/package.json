{
  "name": "contrast-report-runner",
  "version": "1.0.0",
  "private": true,
  "description": "Run color contrast checks for each URL in urls.txt and build a contrast report manifest.",
  "type": "commonjs",
  "scripts": {
    "contrast": "node run_contrast.js",
    "contrast:job": "node run_contrast.js"
  },
  "dependencies": {
    "contrastcheck": "^1.2.0"
  }
}
